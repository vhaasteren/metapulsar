# Core dependencies
numpy>=1.20.0
astropy>=5.0.0
scipy>=1.7.0
# Alpha GitHub pins — same as pyproject.toml. Unpin both for a PyPI v1.0.
# nanograv/PINT#2023 (hybrid PB+FBn -> free FB0) on vhaasteren/PINT@metapulsar.
pint-pulsar @ git+https://github.com/vhaasteren/PINT.git@metapulsar
ephem>=4.1
nltiming @ git+https://github.com/vhaasteren/nltiming.git@main
pyarrow>=14.0.0

# Optional dependencies (uncomment as needed)
# libstempo>=2.0.0
# matplotlib>=3.5.0
# xarray>=0.20.0
# arviz>=0.12.0
# la-forge>=0.1.0
# seaborn>=0.11.0
