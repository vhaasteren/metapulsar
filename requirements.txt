# Core dependencies
numpy>=1.20.0
astropy>=5.0.0
scipy>=1.7.0
# nanograv/PINT#2023 (hybrid PB+FBn -> free FB0). Temporarily track tip of
# vhaasteren/PINT@metapulsar while that PR is in flux; unpin to a release once merged.
pint-pulsar @ git+https://github.com/vhaasteren/PINT.git@metapulsar
ephem>=4.1
# Nonlinear timing stack (extracted from src/metapulsar/timing); from git
# until the first PyPI release.
nltiming @ git+https://github.com/vhaasteren/nltiming.git@45191a68f2ca755bb8a28f83f660a249d485ef08
pyarrow>=14.0.0

# Optional dependencies (uncomment as needed)
# libstempo>=2.0.0
# matplotlib>=3.5.0
# xarray>=0.20.0
# arviz>=0.12.0
# la-forge>=0.1.0
# seaborn>=0.11.0
